name: go-test
on: [push]
jobs:
  go-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.20
          cache: true
      - name: Install dependencies
        run: go env
      - name: Test
        run: go test -v ./...
